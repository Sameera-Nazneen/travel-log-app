import React from 'react';
import Navbar from '../Navbar/Navbar';

const Profile = () => {
    return (
        <div>
            <Navbar/>
            this is profile page
        </div>
    );
};

export default Profile;